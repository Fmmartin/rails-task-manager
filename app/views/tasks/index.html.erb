<ul>
  <% @tasks.each do |task| %>
    <%= link_to(task_path(task)) do %>
      <li><%= task.title %> <%= link_to "🗑",
            task_path(task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
      </li>
    <% end %>
  <% end %>
</ul>

 <%= link_to('Add a new task 🧺', task_new_path) %>
